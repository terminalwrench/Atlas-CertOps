import { ArrowRight, Check, Network, ShieldCheck, Workflow } from 'lucide-react'
import { useEffect } from 'react'
import { Link, useLocation } from 'react-router-dom'
import { documentationUrl, publicAppUrl, supportHref } from '../lib/config'

const plans = [['Free', '10', 'Evaluate one certificate estate'], ['Pro', '100', 'Run certificate operations for a growing team'], ['Team', '500', 'Coordinate multiple operators and customers'], ['Business', '2,500', 'Scale MSP certificate operations']] as const

function SeoSync() { useEffect(() => { const canonical = document.querySelector<HTMLLinkElement>('link[rel="canonical"]'); const ogUrl = document.querySelector<HTMLMetaElement>('meta[property="og:url"]'); if (canonical) canonical.href = publicAppUrl; if (ogUrl) ogUrl.content = publicAppUrl }, []); return null }
function PublicHeader() { return <header className="public-nav"><SeoSync /><Link className="public-brand" to="/"><span className="brand-mark"><ShieldCheck size={20} /></span><strong>Atlas CertOps</strong><small>Private Beta</small></Link><nav aria-label="Public navigation"><Link to="/demo">Live Demo</Link><Link to="/login">Sign In</Link><Link className="button primary" to="/signup">Start Free</Link></nav></header> }
function PublicFooter() { return <footer className="public-footer"><div><strong>Atlas CertOps</strong><span>Certificate lifecycle operations. Metadata only. No private-key custody.</span></div><nav aria-label="Legal and support"><Link to="/privacy">Privacy</Link><Link to="/terms">Terms</Link><Link to="/security">Security</Link><a href={documentationUrl}>Documentation</a><a href={supportHref('Atlas CertOps beta feedback')}>Contact</a></nav></footer> }

export function Landing() { return <div className="public-site"><PublicHeader /><main><section className="public-hero"><div><span className="beta-pill">PRIVATE BETA · EARLY ACCESS</span><h1>Certificate lifecycle operations without enterprise PKI overhead.</h1><p>Know every certificate. Know where it’s deployed. Know what happens when it changes.</p><div className="hero-actions"><Link className="button primary" to="/demo">Explore the live demo <ArrowRight size={15} /></Link><Link className="button secondary" to="/signup">Create a workspace</Link></div><small>Built for MSPs, infrastructure teams, and mixed manual and automated estates.</small></div><div className="operations-preview"><header><span><i /> Renewal in progress</span><small>5 days remaining</small></header><h2>*.customer.example</h2><p>One certificate. Three deployment paths. One external blocker.</p>{['F5 production VIP · In progress', 'Kubernetes ingress · Complete', 'Vendor-managed Citrix · Blocked'].map((item, index) => <div key={item}><span className={index === 1 ? 'done' : index === 2 ? 'blocked' : ''}>{index === 1 ? <Check /> : <Network />}</span>{item}</div>)}</div></section><section className="public-problem"><span className="section-kicker">THE OPERATIONAL LAST MILE</span><h2>Renewing the certificate is only half the job.</h2><p>The replacement still has to reach every load balancer, firewall, VPN, Citrix gateway, Kubernetes ingress, Java keystore, internal application, and vendor-managed system—and then be validated.</p></section><section className="how-grid"><article><Network /><h3>Inventory the estate</h3><p>Map certificate metadata to customers, environments, deployment targets, owners, and dependencies.</p></article><article><Workflow /><h3>Orchestrate replacement</h3><p>Turn every target into an accountable task, including maintenance windows and vendor handoffs.</p></article><article><ShieldCheck /><h3>Validate completion</h3><p>Keep renewals open until required deployments and post-change validations succeed.</p></article></section><section className="audience-section"><div><span className="section-kicker">WHO IT’S FOR</span><h2>Clarity across heterogeneous infrastructure.</h2></div><ul><li>MSPs coordinating certificates across customer environments</li><li>Infrastructure and IT operations teams with shared ownership</li><li>Organizations balancing automation, manual work, and external vendors</li></ul></section><section className="demo-callout"><div><span className="section-kicker">EXPLORE WITHOUT AN ACCOUNT</span><h2>See a complete renewal story in the public demo.</h2><p>Live certificate metadata from curated public endpoints sits beside clearly labeled fictional operations data.</p></div><Link className="button primary" to="/demo">Open live demo <ArrowRight size={15} /></Link></section><section className="pricing-section"><span className="section-kicker">BETA PLAN PREVIEW · PRICING COMING SOON</span><h2>Start small. Scale the certificate estate.</h2><div className="pricing-grid">{plans.map(([name, limit, description]) => <article key={name}><h3>{name}</h3><strong>Up to {limit}</strong><span>certificates</span><p>{description}</p></article>)}</div></section><section className="security-summary"><ShieldCheck /><div><h2>Operational metadata—not private-key custody.</h2><p>Atlas does not issue certificates or store private keys. Production tenant isolation is enforced with Supabase Row Level Security.</p><Link to="/security">Read the security summary <ArrowRight size={14} /></Link></div></section><section className="final-cta"><h2>Make certificate replacement an operation you can see.</h2><p>Join the Atlas CertOps private beta.</p><Link className="button primary" to="/signup">Start free</Link></section></main><PublicFooter /></div> }

const legalCopy = {
  privacy: ['Privacy Policy — Draft', 'This placeholder summarizes the intended metadata-only product boundary. An attorney-reviewed privacy policy is required before broad commercial launch. Atlas CertOps does not intentionally store certificate private keys. Production account and operational metadata are processed through the configured Supabase project.'],
  terms: ['Terms of Service — Draft', 'These terms are a launch-readiness placeholder and are not presented as legal advice or an executed customer agreement. Approved terms, acceptable-use language, warranty limitations, and data-processing provisions are required before paid use.'],
  security: ['Security at Atlas CertOps', 'Atlas stores operational certificate metadata, not certificate private keys. Authenticated workspaces use Supabase Auth and organization-scoped Row Level Security. Public demo inspection is restricted to a curated hostname allowlist and does not accept arbitrary scan targets. No third-party security certification is claimed.'],
} as const
export function TrustPage({ kind }: { kind: keyof typeof legalCopy }) { const [title, copy] = legalCopy[kind]; return <div className="public-site"><PublicHeader /><main className="trust-page"><span className="section-kicker">TRUST CENTER</span><h1>{title}</h1><p>{copy}</p>{kind === 'security' ? <SecurityPageContent /> : <div className="draft-notice">Professional legal review is required before broad commercial launch.</div>}<Link className="button secondary" to="/">Back to Atlas CertOps</Link></main><PublicFooter /></div> }

const concepts = [
  ['Customers & environments', 'Organize certificate operations by the customer and environment where the service runs.'],
  ['Certificates', 'Track non-secret X.509 metadata, ownership, expiry, and renewal method.'],
  ['Deployment targets', 'Map every appliance, ingress, gateway, server, or vendor-managed system that must receive a replacement.'],
  ['Renewals & validations', 'Coordinate deployment tasks and keep work unresolved until required validation passes.'],
  ['Runbooks', 'Capture target-specific replacement steps, warnings, and expected validation.'],
] as const

export function DocumentationPage() { return <div className="public-site"><PublicHeader /><main className="trust-page docs-page"><span className="section-kicker">DOCUMENTATION · GETTING STARTED</span><h1>Operate the certificate lifecycle’s last mile.</h1><p>Atlas CertOps sits alongside certificate authorities and PKI systems. It records where certificates are deployed, who owns replacement work, and whether every target passed validation.</p><section className="trust-section"><h2>Core concepts</h2><div className="docs-concepts">{concepts.map(([title, copy]) => <article key={title}><h3>{title}</h3><p>{copy}</p></article>)}</div></section><section className="trust-section"><h2>Demo and production workspaces</h2><p><strong>Demo:</strong> <code>/demo</code> is public, uses fictional in-memory operations data, and may show separately labeled live TLS metadata from curated public endpoints. Changes reset on refresh.</p><p><strong>Production:</strong> <code>/app</code> requires Supabase authentication and an organization membership. Data is organization-scoped and never falls back to demo fixtures.</p></section><section className="trust-section"><h2>Getting started</h2><ol><li>Create a customer and its environments.</li><li>Add certificate metadata—never a private key.</li><li>Map each location where the certificate is deployed.</li><li>Attach operating runbooks where useful.</li><li>Start a renewal and complete deployment and validation tasks.</li></ol></section><section className="security-callout"><ShieldCheck /><div><strong>Metadata-only boundary</strong><p>Atlas does not issue certificates and does not require or store certificate private keys.</p></div></section><div className="page-actions"><Link className="button primary" to="/demo">Explore the demo</Link><Link className="button secondary" to="/security">Security summary</Link></div></main><PublicFooter /></div> }

export function SecurityPageContent() { return <><section className="trust-section"><h2>Current controls</h2><ul><li><strong>Metadata-only architecture:</strong> Atlas stores operational certificate metadata and does not require or store certificate private keys.</li><li><strong>Authentication boundary:</strong> production workspaces require Supabase Auth and organization membership.</li><li><strong>Tenant isolation:</strong> organization identifiers and PostgreSQL Row Level Security constrain production records.</li><li><strong>Demo isolation:</strong> the anonymous demo uses fictional in-memory fixtures and cannot fall back to or query a production tenant.</li><li><strong>Auditability:</strong> lifecycle actions use the existing append-oriented audit event model.</li><li><strong>Transport:</strong> the deployed application and configured Supabase services use HTTPS/TLS in transit.</li></ul></section><section className="trust-section"><h2>Current limitations</h2><p>Atlas is in private beta. It does not claim a formal compliance certification, guaranteed uptime, completed penetration test, or private-network collector capability. Public TLS inspection is limited to curated allowlisted endpoints and is separate from fictional demo workflows.</p></section><section className="trust-section"><h2>Security roadmap</h2><p>Planned work includes additional security review, operational monitoring, documented incident procedures, and enterprise identity options. Roadmap items are not represented as completed controls.</p></section></> }

export function ContactPage() { return <div className="public-site"><PublicHeader /><main className="trust-page"><span className="section-kicker">BETA SUPPORT</span><h1>Talk to the Atlas team.</h1><p>Support and feedback contact is configured per deployment. This environment does not yet publish a support address.</p><p className="draft-notice">Set <code>VITE_SUPPORT_EMAIL</code> before public beta to enable direct support and feedback links.</p><Link className="button secondary" to="/demo">Explore the demo</Link></main><PublicFooter /></div> }

export function NotFoundPage({ workspace = false }: { workspace?: boolean }) { const { pathname } = useLocation(); const home = pathname.startsWith('/app') ? '/app' : pathname.startsWith('/demo') ? '/demo' : '/'; return <div className={workspace ? 'not-found workspace-not-found' : 'public-site'}>{!workspace && <PublicHeader />}<main className="not-found-content"><span className="section-kicker">404 · ROUTE NOT FOUND</span><h1>This page isn’t in the Atlas map.</h1><p>The address may be outdated, or the operational object may no longer exist.</p><div className="page-actions"><Link className="button primary" to={home}>Return home</Link><Link className="button secondary" to="/demo">Open demo</Link></div></main>{!workspace && <PublicFooter />}</div> }
